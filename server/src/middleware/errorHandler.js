module.exports = (err, req, res, next) => { let status = err.statusCode || 500; let message = err.message || 'Internal server error'; if (err.name === 'CastError') { status = 400; message = 'Invalid resource identifier'; } if (err.code === 11000) { status = 409; message = `Duplicate value for ${Object.keys(err.keyPattern || {}).join(', ') || 'unique field'}`; } if (err.name === 'ValidationError') { status = 400; message = Object.values(err.errors).map((e) => e.message).join(', '); } res.status(status).json({ success: false, message, error: status === 500 && process.env.NODE_ENV === 'production' ? 'Internal server error' : message }); };
