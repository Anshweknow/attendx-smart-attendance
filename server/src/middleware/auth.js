const jwt = require('jsonwebtoken'); const { User } = require('../models'); const AppError = require('../utils/AppError'); const asyncHandler = require('../utils/asyncHandler'); const config = require('../config/env');
exports.authenticateUser = asyncHandler(async (req, res, next) => { const token = req.headers.authorization?.startsWith('Bearer ') && req.headers.authorization.slice(7); if (!token) throw new AppError('Authentication token is required', 401); let payload; try { payload = jwt.verify(token, config.jwtSecret); } catch { throw new AppError('Invalid or expired authentication token', 401); } const user = await User.findById(payload.userId); if (!user) throw new AppError('User no longer exists', 401); req.user = user; next(); });
exports.requireRole = (...roles) => (req, res, next) => { if (!req.user || !roles.includes(req.user.role)) return next(new AppError('You do not have permission for this action', 403)); next(); };
